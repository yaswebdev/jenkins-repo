pipeline {
    agent any  // means use the default node
    stages {
        stage('Hello Python') {
            steps {
                sh 'python3 -c "print(\'Hello World\')"'
            }
        }
    }
}
